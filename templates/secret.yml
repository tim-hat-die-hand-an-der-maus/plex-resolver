apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
data:
  AMQP_USER: "{{ .Values.secret.username }}"
  AMQP_PASSWORD: "{{ .Values.secret.password }}"
  PLEX_TOKEN: "{{ .Values.secret.token }}"